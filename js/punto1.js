let num = parseFloat(prompt("Digite un número"))
let positivo = num * -1

if (num < 0 ){
    document.write(num + " y su número positivo: ", positivo)
    
}
else {
        if(num > 0 )
        document.write("su número positivo: " + num )
    }
